import { Github, LockKeyhole } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Status } from '../design-system'
export function SignInPage() { return <main className="page" style={{ maxWidth: 620 }}><p className="eyebrow">Welcome to SchemaSprint</p><h2 style={{ fontSize: 'clamp(2rem,5vw,3.5rem)', letterSpacing: '-.06em', lineHeight: 1 }}>Practice the decisions behind good data models.</h2><p className="muted">Sign in with the identity you already use. The service keeps a separate internal account ID and never relies on provider display names.</p><section className="panel panel-pad stack" style={{ marginTop: 28 }}><h3 style={{ margin: 0 }}>Choose a provider</h3><a className="btn btn-primary" href="/api/v1/auth/google/start">Continue with Google</a><a className="btn btn-subtle" href="/api/v1/auth/github/start"><Github size={17} />Continue with GitHub</a><Status tone="info" title="Security gates">Only configured allowlisted identities can complete onboarding. Age and current policy acknowledgement are required before learning.</Status><Link to="/" className="small muted">Continue browsing without signing in</Link></section><p className="small muted" style={{ marginTop: 18 }}><LockKeyhole size={14} /> No password is stored by SchemaSprint.</p></main> }
