[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "schemasprint-core"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
features = ["extension-module", "abi3-py312"]
module-name = "schemasprint_core"
